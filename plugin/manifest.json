{
  "name": "Figma-migrate",
  "id": "figma-migrate-v0-1",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "enableProposedApi": true,
  "networkAccess": {
    "allowedDomains": [
      "https://cdn.jsdelivr.net",
      "https://fonts.googleapis.com",
      "https://fonts.gstatic.com"
    ],
    "reasoning": "Loads html2canvas (DOM → image library) so the DS Candidate picker can render selected HTML elements as images and embed them in the build output. Google Fonts domains support both the plugin UI's own typography and rendering of user-uploaded HTML in the picker iframe."
  }
}
