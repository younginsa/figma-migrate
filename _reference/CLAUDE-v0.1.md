# HiNAS Control — Figma migration project

When working in this folder, the migration runbook below is ambient context. The full rule set (rationale, code samples, master keys) lives at `.claude/commands/figma-migrate.md` and is invoked via the `/figma-migrate` slash command.

## Always-on rules

@.claude/always-on-rules.md

## Auto-loaded checklist (figma-migrate workflow)

@.claude/figma-migrate-checklist.md

## Project artifacts

- Figma file: `iSBKt82nnX2Uebb1hH4AfE` (Control DS — Claude x Figma)
- Working page: `Claude output`
- DS manifest cache: `.claude/ds-manifest.json` (compared each run)
- Migration outputs: `Result` section on the working page
